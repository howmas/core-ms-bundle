{% set type = definition.fieldtype %}
{% set type = type ?: hcore_form_check_type(definition) %}

{% if type and not definition.invisible %}
  {% set typePath = ('/layout/form/' ~ (type|lower) ~ '.html.twig')|hcore_fullpath %}

  {# {% if include(typePath) is defined %} #}
    {% set typeConfig = hcore_form_type_config(type) %}
    {{ include(typePath, {item: item, definition: definition, typeConfig: typeConfig}) }}
  {# {% endif %} #}
{% endif %}
