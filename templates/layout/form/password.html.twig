{% extends HCoreFormLayout %}

{% block content %}
  <input type="password" {%if noteditable%}disabled{%else%}name="{{ name }}" placeholder="{{ placeholder }}"{%endif%} id="{{ labelId }}" class="form-control {{ hcorePasswordCls }}" value="********">
{% endblock %}
